@{
    var userId = Context.Session.GetInt32("UserId");

    if (userId != null)
    {
        Layout = "_Layout"; // logged-in layout
    }
    else
    {
        Layout = "_IndexLayout"; // guest layout
    }
}
